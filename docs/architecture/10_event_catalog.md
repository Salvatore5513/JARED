# Event catalog (discovered)

This is auto-discovered by scanning for `EventBus.emit('...')` calls.

- `action.failed` (seen 4×)
- `action.executed` (seen 2×)
- `action.requested` (seen 1×)
- `action.policy_decided` (seen 1×)
- `action.verified` (seen 1×)
